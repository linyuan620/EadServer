<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.temolin.myapplication">

    <application
        android:name="com.temolin.application.Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.temolin.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.temolin.WifiHotManager.WiFiAPService">
        </service>

        <service android:name="com.temolin.service.SerialPortService">
        </service>

        <receiver android:name="com.temolin.BroadcastReceiver.MyBroadcastReceive">
　　        <intent-filter>
　　           <action android:name="android.intent.action.BOOT_COMPLETED"/>
　　        </intent-filter>
　　        </receiver>

    </application>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
</manifest>
